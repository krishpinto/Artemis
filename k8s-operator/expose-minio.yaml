apiVersion: v1
kind: Service
metadata:
  name: ui-minio-svc
  namespace: default
spec:
  selector:
    app: ui-minio
  ports:
    - name: console
      protocol: TCP
      port: 9001
      targetPort: 9001
  type: NodePort