apiVersion: v1
kind: Service
metadata:
  name: ui-grafana-svc
  namespace: default
spec:
  selector:
    app: ui-grafana
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: NodePort