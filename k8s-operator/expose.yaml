apiVersion: v1
kind: Service
metadata:
  name: my-database-postgres-svc
  namespace: default
spec:
  selector:
    app: my-database-postgres
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
  type: NodePort